/**
 * DCard — Modern real estate ERP card and table styles.
 * High contrast, legible typography, responsive padding.
 */
export const cardStyle = {
  backgroundColor: 'var(--df-card-bg)',
  border: '1px solid var(--df-card-border)',
  borderRadius: '10px',
  overflow: 'hidden',
  boxShadow: 'var(--df-shadow-sm)',
};

export const cardHeaderStyle = {
  padding: '12px 16px',
  borderBottom: '1px solid var(--df-border)',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
};

export const cardTitleStyle = {
  fontSize: '0.88rem',
  fontWeight: 700,
  letterSpacing: '-0.01em',
  color: 'var(--df-text)',
};

export const cardSubtitleStyle = {
  fontSize: '0.78rem',
  color: 'var(--df-text-muted)',
  marginTop: '2px',
};

export const cardBodyStyle = {
  padding: '16px',
};

export const thStyle = {
  fontSize: '0.72rem',
  fontWeight: 700,
  textTransform: 'uppercase',
  letterSpacing: '0.05em',
  color: 'var(--df-text-muted)',
  padding: '11px 16px',
  backgroundColor: 'var(--df-bg)',
  borderBottom: '1px solid var(--df-border)',
  textAlign: 'left',
  whiteSpace: 'nowrap',
};

export const tdStyle = {
  fontSize: '0.85rem',
  color: 'var(--df-text)',
  padding: '12px 16px',
  borderBottom: '1px solid var(--df-border)',
  verticalAlign: 'middle',
};

export const linkBtnStyle = {
  fontSize: '13px',
  fontWeight: 600,
  color: 'var(--df-accent)',
  background: 'transparent',
  border: 'none',
  cursor: 'pointer',
  padding: 0,
  textDecoration: 'none',
};

